apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
   name: wikihomez
   namespace: thymeleaf
spec:
   ingressClassName: public
   rules:
   - host: gruposjaveriana.dynaco.co
     http:
       paths:
       - path: /wikihomez
         pathType: Prefix
         backend:
           service:
             name: wikihomez
             port:
               number: 8080